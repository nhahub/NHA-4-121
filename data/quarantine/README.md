# Quarantine Data

This folder stores rejected patient JSON files and validation/audit issue reports.

Files in this folder must never be ingested into ChromaDB.

If this folder is empty, it means the generated dataset passed validation successfully.
